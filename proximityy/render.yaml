services:
  - type: web
    name: proximity-chat
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --worker-class gthread --threads 100 app:app
    envVars:
      - key: SECRET_KEY
        generateValue: true
